cmake_minimum_required(VERSION 3.0.2)
project(lsrx_rm75_dual_description)

find_package(catkin REQUIRED)

catkin_package()

foreach(directory config launch meshes urdf)
  install(
    DIRECTORY ${directory}/
    DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION}/${directory}
  )
endforeach()
