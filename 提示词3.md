
你现在需要继续开发我的 **VR 遥操双睿尔曼 RM75 机械臂项目**，完成“第一次真实低速运动之前”的最后一轮 commissioning 软件工作。

GitHub 最新仓库：

https://github.com/moyunon/vr_teleop.git

本地项目预计位于：

```bash
~/workspace/vr_teleop
```

ROS 2 工作区：

```bash
~/workspace/vr_teleop/vr_rm75_ws
```

---

# 一、你的总体任务

目前项目已经不再处于“搭建 IK / VR 映射 / 基础安全逻辑”的阶段。

现在已经进入：

> **真实双 RM75 系统 commissioning / Hardware-in-the-loop 联调阶段**

你的任务不是继续泛化地增加安全功能，而是围绕：

1. 真实 RM75 状态反馈；
2. 明确的机器人停止路径；
3. 实际碰撞距离计算；
4. 跟随误差、实测速度/加速度与时序监控；
5. 一键 dry-run 和现场 commissioning 流程；

把当前软件推进到：

> **可以安全进行“只读上机 → 完整 dry-run → 极低速单臂首次运动”的状态。**

---

# 二、最高优先级安全约束

这些要求是硬约束，不允许自行修改。

## 1. 禁止自行打开真实运动

当前配置中的：

```yaml
enable_robot_motion: false
```

必须继续保持默认值为：

```yaml
false
```

你不得：

* 自动改成 true；
* 为了测试临时改成 true；
* 在 pytest 中连接真实命令 socket；
* 主动向真实机械臂发送运动指令；
* 主动执行真实 `movej_canfd`；
* 主动执行真实首次运动；
* 通过 launch 默认连接真实运动输出。

即使所有代码开发完成，也必须由现场操作者最后显式开启真实运动。

---

## 2. 允许真实只读联调

可以为以下目的完善代码：

* RM75 TCP 只读状态；
* UDP realtime state；
* joint position；
* joint speed；
* error；
* enable；
* controller state；
* timestamp；
* network latency；
* stale detection。

但不要擅自连接运动输出通道。

---

## 3. 不允许猜测真实机械尺寸

涉及：

* 环境；
* 机械臂安装基座；
* 机器人主体；
* 相机；
* 相机支架；
* 桌面；
* 固定障碍；

如果仓库、URDF 或已有配置没有给出可靠尺寸：

**不要猜。**

正确处理方式是：

1. 实现参数化接口；
2. 清楚标记缺失数据；
3. collision backend 保持 NOT READY；
4. Safety Supervisor 继续 fail-closed；
5. 最终报告中列出现场需要测量的数据。

---

## 4. 不得删除已有安全保护

包括但不限于：

* deadman；
* clutch；
* stale watchdog；
* joint soft limits；
* joint velocity limiter；
* qddot limiter；
* singularity protection；
* IK failure protection；
* collision fail-closed；
* dual-arm fault linking；
* command jump check；
* NaN/Inf protection；
* measured-state initialization。

不得为了方便调试将这些保护旁路掉。

---

# 三、先不要直接修改代码

你必须首先阅读当前仓库。

第一阶段只进行审计。

至少执行并检查：

```bash
cd ~/workspace/vr_teleop

git status
git branch --show-current
git log -1 --oneline
git diff
```

不要：

```bash
git reset --hard
git checkout .
git clean -fd
```

不要覆盖用户已有未提交修改。

---

# 四、优先阅读这些文件

至少阅读：

```text
vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/rm75_hardware_interface.py

vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/rm75_state_node.py

vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/arm_fusion_state.py

vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/safety_supervisor.py

vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/deadman_clutch.py

vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/collision_safety.py

vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/rm75_command_interface.py

vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/quest_dual_ik_fusion.py

vr_rm75_ws/src/vr_rm75_teleop/config/safe_first_motion.yaml
```

同时检查：

```text
launch/
config/
test/
tests/
URDF/
meshes/
package.xml
setup.py
setup.cfg
README
```

以及当前碰撞、状态和命令接口相关的所有 launch/config。

不要只根据文件名判断。

要真正沿调用关系阅读。

---

# 五、已有功能，不要重复实现

目前已经完成：

* 双 RM75 FK；
* Jacobian；
* DLS IK；
* 连续 IK；
* VR → ARM pose mapping；
* world-space Quest pose；
* measured q 初始化；
* VR anchor/re-anchor；
* deadman；
* clutch；
* READY / ENGAGED / HOLD / FAULT 状态机；
* joint hard/soft limit；
* J4 elbow branch protection；
* qdot limiter；
* qddot limiter；
* command jump detection；
* singularity safe/warning/stop；
* IK projection；
* collision safety consumer interface；
* collision stale fail-closed；
* RM75 TCP state software interface；
* RM75 command software interface；
* watchdog；
* dual arm fault linking；
* first-motion conservative config；
* fake socket tests。

现有功能已经有测试证据时：

> 不要重写。

只有发现明确 bug 时才能修改，并说明原因。

---

# 六、Adaptive damping 不属于本轮任务

当前 adaptive damping 离线测试已经证明：

在 `sigma_stop` 附近的稳定性反而低于固定 damping。

因此：

```text
继续使用现有 fixed DLS
```

不要：

* 重写 adaptive damping；
* 再设计一套 damping；
* 修改现有 IK 主算法；
* 把本轮 commissioning 工作变成 IK 优化工作。

---

# 七、首先输出一份实施计划

在修改代码之前，先给出：

## A. 当前真实数据流

至少整理：

```text
Quest
 ↓
VR pose / grip
 ↓
mapping
 ↓
IK
 ↓
limits / singularity
 ↓
collision
 ↓
SafetySupervisor
 ↓
safe command
 ↓
RM75 command dispatcher
 ↓
RM75

RM75 feedback
 ↑
state interface
```

说明真实实现是否与此一致。

---

## B. 当前四个主要 commissioning 缺口

重点检查：

1. RM75 realtime feedback 是否真正可用于闭环安全；
2. HOLD/FAULT 是否真正触发明确 stop；
3. collision backend 是否真正计算实际距离；
4. q_command ↔ q_measured 和 control-loop timing 是否已实际监控。

---

## C. 给出计划修改文件

明确列出：

```text
修改：
新增：
不修改：
```

---

## D. 给出实施顺序

必须遵循：

```text
Task A
↓
Task B
↓
Task C
↓
Task D
↓
完整测试
↓
最终 commissioning 报告
```

给出计划后直接开始实施。

除非发现必须依赖真实现场尺寸等无法从仓库获得的信息，否则不要停下来等待用户确认。

遇到现场信息缺失时：

> 完成可以完成的软件接口和 fail-closed 机制，并将缺失项记录为 HARDWARE_PENDING。

不要凭空填值。

---

# 八、Task A —— Real RM75 State / Realtime Feedback

目标：

> 将当前“软件模拟通过的 RM75 state interface”推进到真正适合现场 commissioning 的状态。

---

## A1. 审计当前 TCP state path

确认：

* joint 顺序；
* joint 数量；
* 单位；
* JSON parser；
* error field；
* enable field；
* brake field；
* controller status；
* timeout；
* reconnect；
* NaN/Inf；
* malformed packet；
* stale。

不要仅检查 happy path。

---

## A2. 加强 UDP realtime state

当前系统已有可选被动 UDP 支持时：

不要重新另写一个无关节点。

优先扩展现有实现。

目标至少支持获取：

```text
joint_position
joint_speed
joint_error
joint_enable
robot/controller error
timestamp / reception time
```

如果官方当前协议字段与现有 parser 不一致：

以当前官方协议和真实仓库实现为准，并清楚记录。

不要凭记忆猜 JSON 字段。

---

## A3. UDP 必须具有来源校验

至少检查：

```text
source IP
packet validity
joint count
finite number
timestamp
packet age
```

不能接受：

* 任意来源 UDP；
* malformed；
* NaN；
* Inf；
* joint 数量错误；

并将其作为有效 robot state。

---

## A4. 区分 ROS publish rate 和真实 measurement rate

当前系统不能因为：

```text
ROS topic = 50 Hz
```

就声称：

```text
robot measurement = 50 Hz
```

需要记录真正的新 measurement：

```text
measurement_seq
measurement_receive_time
measurement_age
measurement_period
```

最好加入：

```text
effective_measurement_hz
```

或者等价诊断。

---

## A5. measured qdot

如果 RM75 UDP 提供官方 joint_speed：

优先直接使用：

```text
qdot_measured
```

不要默认通过：

```text
(q[k] - q[k-1]) / dt
```

得到 qdot。

差分只能作为：

* fallback；
* commissioning debug；

而且必须标识其质量低于直接反馈。

---

## A6. 状态质量诊断

增加合理的 diagnostic 信息：

至少能看到：

```text
LEFT:
connected
transport
state_age
measurement_hz
last_receive_age
joint_valid
controller_fault
stale

RIGHT:
同上
```

不要刷屏。

使用节流日志或者 diagnostic topic。

---

## A7. Task A 测试

需要增加/完善：

```text
valid TCP
valid UDP
wrong source IP
wrong joint count
NaN
Inf
malformed JSON
timeout
stale
reconnect
duplicate measurement
UDP → TCP fallback
```

如果 fallback 机制存在。

所有测试必须使用：

```text
fake socket / local socket
```

不得连接真实机器人。

---

# 九、Task B —— Explicit Robot Stop Path

这是本轮最高优先级安全任务之一。

目前需要重点确认：

> Safety Supervisor 进入 HOLD/FAULT 后，是不是仅仅停止继续下发 target。

如果只是：

```text
disarm
```

或者：

```text
停止 send movej_canfd
```

那么这还不是完整 stop semantics。

---

# 十、Task B 的目标

实现明确：

```text
Safety Event
    ↓
Stop Policy
    ↓
RM75 explicit stop command
```

---

## B1. 首先审计 RM75 官方 stop API

使用当前官方睿尔曼文档确认：

* controlled slow stop；
* arm stop；
* emergency stop；

等接口的：

```text
命令名称
JSON 格式
返回格式
适用条件
恢复要求
```

不要根据记忆实现。

---

## B2. Stop 必须分级

设计至少两个概念：

```text
CONTROLLED_STOP
SAFETY_STOP
```

示意：

### CONTROLLED_STOP

适用于例如：

```text
正常 deadman release
正常退出 ENGAGED
operator clutch release
```

具体映射哪个 RM75 stop API，由官方 API 和当前系统语义决定。

---

### SAFETY_STOP

适用于例如：

```text
collision stop
robot state stale
command timeout
NaN / Inf
severe IK fault
joint safety violation
qdot violation
qddot violation
command jump
controller fault
dual-arm send failure
```

应该映射到更强的明确安全停止路径。

---

## B3. 不要把 physical E-stop 当成软件 stop

文档中必须明确：

```text
software stop
≠
physical emergency stop
```

尤其是：

```text
network disconnected
process crash
computer freeze
```

时，PC 软件不一定还能发 stop。

现场仍然需要：

> 实体急停 / 机械臂原生安全机制作为最终兜底。

---

## B4. Stop command 必须 edge-triggered

不要在 50 Hz 主循环中变成：

```text
stop
stop
stop
stop
stop
...
```

应该：

```text
ENGAGED
 ↓ safety event
HOLD
 ↓
send stop once
 ↓
record stop result
```

必要时允许受控 retry。

---

## B5. 双臂 fault linking

如果：

```text
LEFT command failure
```

则不能只让 LEFT 进入异常而 RIGHT 继续运动。

要求：

```text
LEFT failure
 ↓
attempt stop LEFT
attempt stop RIGHT
 ↓
system FAULT
```

反之亦然。

---

## B6. stop response 必须监控

至少记录：

```text
stop_requested
stop_type
stop_reason
request_timestamp
ack
ack_latency
timeout
error_code
```

不能假设：

```text
socket.send() 成功
```

就等于：

```text
robot stopped
```

真实“停止距离”和“机器人是否真正静止”属于 HARDWARE_PENDING。

---

## B7. shutdown behavior

进程退出时：

如果：

```text
当前曾处于 motion armed
AND
command socket 已存在
```

可以做 best-effort stop。

但：

> 不允许为了 shutdown stop 而临时新建真实 command connection。

软件无法保证：

```text
SIGKILL
kernel panic
network loss
power loss
```

时还能发送 stop。

文档必须明确这一限制。

---

## B8. dry-run 行为

当：

```yaml
enable_robot_motion: false
```

时：

任何 stop 测试都只能：

```text
simulate/log intended stop
```

或者使用 fake transport。

不能因为 stop 是“安全命令”就绕过运动总开关主动连接真实 command socket。

---

## B9. Task B 测试

至少测试：

```text
deadman release
VR stale
collision stop
robot stale
IK severe failure
qdot violation
qddot violation
command timeout
LEFT send failure
RIGHT send failure
stop ACK
stop timeout
stop malformed response
repeated HOLD does not spam stop
re-arm requires valid release/re-engage
```

---

# 十一、Task C —— Actual Collision Distance Backend

当前已有：

```text
collision safety interface
```

但缺：

```text
真正距离计算
```

本任务必须实现。

---

# 十二、首先做 URDF Collision Audit

检查当前机器人模型：

每个 link 的：

```xml
<visual>
<collision>
```

不要默认二者一致。

生成一份审计结果：

```text
link
visual geometry
collision geometry
mesh/primitives
missing?
obviously inaccurate?
```

重点检查：

```text
base
shoulder
upper arm
forearm
wrist
flange
end effector
camera
camera bracket
mount
```

---

# 十三、末端相机不能被忽略

当前实际机械臂末端存在相机。

如果 URDF 没有相机碰撞体：

不要假装不存在。

优先：

1. 查看仓库是否已有准确 mesh；
2. 查看已有 URDF/Xacro；
3. 查看已有尺寸。

如果都没有：

实现参数化 camera collision geometry；

但不要编造尺寸。

并将系统标记：

```text
collision_geometry_ready = false
```

直到实际尺寸补齐。

---

# 十四、选择碰撞后端

候选：

```text
FCL
MoveIt Planning Scene
其他已有可靠 collision library
```

本阶段不需要因为“以后可能规划”就强行引入完整 MoveIt。

你的选择标准应该是：

```text
能稳定获取 minimum distance
容易测试
运行开销可接受
与当前 ROS2 / Python 架构兼容
依赖清晰
```

如果轻量 FCL 足够：

优先轻量方案。

但先检查当前环境和依赖，不要机械照搬建议。

不要使用自己手写的简陋球体距离替代真正 collision geometry，除非只是显式 debug fallback。

---

# 十五、必须提供五类距离

和当前 collision consumer 完全兼容：

```text
1. LEFT self collision distance

2. RIGHT self collision distance

3. LEFT ↔ RIGHT minimum distance

4. robot ↔ environment minimum distance

5. robot ↔ robot body / mounting structure minimum distance
```

发布必须是：

> 同一个周期、同一份 snapshot。

不能五个 topic 分别到达后拼成时间不一致的数据。

---

# 十六、self-collision pair filtering

需要正确处理：

```text
adjacent links
structurally connected links
always-contact geometry
```

不能把正常相邻 link 接触误判为：

```text
self collision = 0
```

需要明确的：

```text
allowed / ignored collision pair
```

配置或等效机制。

---

# 十七、双臂基坐标关系

双臂 collision 必须使用真实：

```text
LEFT base
↔
RIGHT base
```

固定变换。

检查：

* 当前 URDF；
* TF；
* mounting transform；
* side mount configuration。

如果变换没有可靠来源：

不得猜测。

标记 HARDWARE_PENDING。

---

# 十八、环境与机器人主体

collision backend 最好支持参数化 primitives：

```text
box
cylinder
sphere
mesh
```

例如未来可描述：

```text
table
pedestal
robot body
wall
camera support
```

但配置中不得虚构现场尺寸。

缺少必须 geometry 时：

```text
collision_ready = false
```

Supervisor 继续 fail-closed。

---

# 十九、collision snapshot 必须包含质量信息

至少：

```text
timestamp
age
valid
ready
minimum distance values
closest category
```

最好可选记录：

```text
closest link pair
```

方便 commissioning。

---

# 二十、Collision Debug Visualization

如果不会显著破坏现有结构，增加 RViz debug：

```text
collision geometry
closest points
warning
stop
```

或者至少提供：

```text
closest pair diagnostics
```

这样第一次现场调试可以核对：

> 数学模型认为最近的位置，到底是不是现实中最近的位置。

---

# 二十一、collision timing

记录 collision backend：

```text
solve time
mean
max
```

并为后续 Task D timing instrumentation 提供数据。

不要目前凭空设置“必须小于 X ms”。

先测。

---

# 二十二、Task C 测试

至少包括：

```text
separated geometry
warning distance
stop distance
actual overlap
self collision
inter-arm collision
environment collision
missing geometry
stale snapshot
invalid NaN
transform unavailable
adjacent-link exclusion
```

---

# 二十三、Task D —— Closed-loop Commissioning

Task D 包含四部分：

```text
D1 following error
D2 measured qdot/qddot
D3 timing / jitter
D4 unified dry-run
```

---

# 二十四、D1 —— Following Error

实现：

```text
q_error = q_command - q_measured
```

需要正确处理：

* joint representation；
* angular wrap；
* joint limits；
* timestamp mismatch。

不要简单比较不同时间对应的：

```text
q_command[k]
```

和：

```text
过旧 q_measured
```

而不检查 state age。

---

## Following Error 状态

至少：

```text
NORMAL
WARNING
STOP
```

并有：

```text
hysteresis
persistence time
```

避免单个 packet 抖动立即触发 FAULT。

---

## 不要伪造最终 threshold

目前真实机械臂：

* tracking latency；
* controller interpolation；
* network delay；

还没有实测。

所以：

可以提供：

```text
conservative commissioning defaults
```

但必须清楚注释：

```text
PROVISIONAL
REQUIRES HARDWARE CALIBRATION
```

不得把其写成“已验证安全值”。

---

# 二十五、D2 —— 实测速度与加速度

优先：

```text
qdot_measured = RM75 UDP joint speed
```

如果 UDP 没有有效 qdot：

fallback 差分必须明确标记。

---

## qddot_measured

允许：

```text
filtered derivative(qdot_measured)
```

但必须考虑：

```text
noise
dt jitter
stale packet
duplicate packet
```

不要：

```text
(qdot[k]-qdot[k-1]) / nominal_0.02
```

强制假设固定 dt。

必须使用实际 measurement dt。

---

## qddot 过滤

实现简单、可解释、可调的 filter。

不要为了这一项加入复杂 estimator。

---

# 二十六、D3 —— Timing / Jitter Instrumentation

必须记录实际运行时序。

至少：

```text
VR input age

RM75 state age

IK solve time

collision solve time

supervisor processing time

command send time

LEFT command ACK RTT

RIGHT command ACK RTT

full control cycle time

deadline miss count
```

如果某个阶段当前架构无法单独测量：

解释原因。

---

## 统计

至少支持：

```text
mean
max
```

最好增加滑动窗口：

```text
p95
p99
```

不要为此引入重量级依赖。

---

## Deadline

当前 nominal：

```text
50 Hz
20 ms
```

但不要因为配置写的是 50 Hz 就声称系统已满足 50 Hz。

需要真正报告：

```text
effective frequency
deadline misses
jitter
```

---

# 二十七、D4 —— Unified Dry-run Launch

需要提供一个统一 commissioning launch。

目标类似：

```text
Quest bridge
+
LEFT RM75 state
+
RIGHT RM75 state
+
IK / fusion
+
collision backend
+
Safety Supervisor
+
diagnostics
```

默认：

```yaml
enable_robot_motion: false
```

---

## Dry-run launch 必须保证

即使用户：

```text
戴上 Quest
按下 Grip
进入 ENGAGED
```

系统仍然：

```text
只生成 safe command
不连接/不发送真实运动
```

---

## Launch 参数

至少考虑：

```text
robot IP
TCP port
UDP port
collision config
environment config
enable diagnostics
enable bag/logging
enable_robot_motion
```

但不要过度复杂化。

---

# 二十八、Commissioning Recorder

提供方便记录关键 topic 的方式。

可以是：

```text
ros2 bag record command
```

或轻量脚本。

至少记录：

```text
VR poses
grip/deadman
q_measured
qdot_measured
q_command
safe command
sigma
IK state
collision distances
safety state
stop events
robot status
timing diagnostics
```

这样后续首次运动能离线分析。

---

# 二十九、增加现场 Commissioning Checklist

新增一份文档，例如：

```text
docs/COMMISSIONING_CHECKLIST.md
```

或者合理的现有 docs 目录。

必须明确分成：

---

## Gate A —— Read-only Hardware Check

保持：

```text
enable_robot_motion = false
```

检查：

```text
LEFT IP
RIGHT IP
joint count
joint order
joint units
joint signs
enable
errors
brake
state age
UDP rate
TCP latency
```

要求操作者可以人工核对：

```text
真实 J1 + 小幅运动
→ ROS J1 符号一致

J2
...
J7
```

注意：

文档可以要求现场做。

你自己不要做。

---

## Gate B —— Hardware-in-the-loop Dry-run

仍然：

```text
enable_robot_motion = false
```

测试：

```text
INIT
READY
ENGAGED
HOLD
FAULT
```

以及：

```text
deadman release
VR loss
Grip stale
collision stale
collision stop
RM75 stale
IK failure
soft limit
singularity
```

要求确认：

```text
robot_command_sent = false
```

---

## Gate C —— Command Channel Commissioning

不要直接使用 VR。

文档要求现场：

```text
q_target = q_measured + tiny_delta
```

做极小关节运动。

建议提供一个：

```text
commissioning_joint_step
```

工具或脚本。

但它必须：

```text
DEFAULT DRY RUN
```

并且真实发送必须要求非常明确的显式参数。

例如：

```text
--enable-real-send
```

以及：

```text
arm
joint
delta
```

需要有硬限制。

例如不允许大 step。

具体限制根据现有单位和 API 合理设计。

你不要实际执行 real-send。

---

# 三十、Command Commissioning 现场必须验证

文档要求测试：

```text
tiny movement direction
command ACK
RTT
stop sending behavior
CONTROLLED_STOP behavior
SAFETY_STOP behavior
left/right mapping
```

必须特别强调：

> “停止发送 movej_canfd 后机械臂实际怎么停”

需要实测。

不能由软件开发阶段推断。

---

# 三十一、Gate D —— First VR Motion

只有 A/B/C 全部通过之后。

并且由操作者显式：

```yaml
enable_robot_motion: true
```

第一次：

```text
单臂
低速
空载
清空工作区
急停人员就位
```

步骤：

```text
1. VR 静止
2. Grip
3. 确认机械臂不跳
4. Release
5. 重新 Grip
6. 单独 +X 约 1 cm
7. Release
```

然后：

```text
-X
+Y
-Y
+Z
-Z
```

再：

```text
roll ±
pitch ±
yaw ±
```

最后才允许：

```text
双臂
```

---

# 三十二、建立明确 Stop Policy 文档

建议新增：

```text
docs/STOP_POLICY.md
```

至少描述：

```text
event
software state
stop type
dual-arm behavior
re-arm condition
hardware limitation
```

例如结构：

| Event | State | Stop class | Both arms? | Re-arm |
| ----- | ----- | ---------- | ---------- | ------ |

但具体 RM75 指令必须基于你查到的当前官方 API。

---

# 三十三、代码结构要求

不要把所有新功能继续塞进：

```text
quest_dual_ik_fusion.py
```

如果逻辑已经明显独立：

拆成小模块。

例如可能形成：

```text
robot_feedback_monitor.py
following_error_monitor.py
timing_monitor.py
collision_backend.py
stop_policy.py
```

名称根据现有工程风格决定。

但不要为了“架构漂亮”大规模重构现有系统。

原则：

> 最小必要修改。

---

# 三十四、不要处理无关事项

本轮不处理：

```text
全仓库 155 个 Flake8
全仓库 101 个 PEP257
历史数学脚本格式
全项目 formatter
大规模 rename
README 全面重写
IK 重构
adaptive damping
高 follow 模式
控制频率升级
提高速度
```

只要求：

> 本次新增和修改文件风格检查通过。

---

# 三十五、不要擅自提高安全参数

当前首次运动配置例如：

```text
translation ≈ 0.05 m/s
rotation ≈ 0.30 rad/s
joint velocity scale ≈ 0.10
qddot conservative
```

不要主动提高。

如果发现某参数和代码语义不一致：

说明问题。

不要自行“优化得更快”。

---

# 三十六、依赖管理

如果碰撞 backend 需要新的库：

先检查当前环境是否已有。

如果必须引入：

说明：

```text
为什么需要
版本
ROS package dependency
Python dependency
runtime dependency
```

优先通过：

```text
package.xml
setup.py / requirements
README/doc
```

规范表达。

不要：

```bash
sudo apt install ...
sudo pip install ...
```

除非用户明确允许你修改系统环境。

如果依赖缺失导致本地无法实际运行：

仍然完成：

```text
代码
接口
mock/unit tests
依赖声明
安装说明
```

并在最终报告中标记：

```text
BLOCKED_BY_DEPENDENCY
```

不要假装测试成功。

---

# 三十七、Build / Workspace Hygiene

检查仓库是否误跟踪：

```text
build/
install/
log/
```

如果存在：

先判断：

```text
是否已经被 Git track
.gitignore 当前策略
是否会造成 source 旧 install 的风险
```

不要贸然：

```bash
rm -rf
```

用户文件。

如果确实应该从版本控制中移除：

只做合理 Git hygiene 修改，并说明。

确保最终说明如何验证：

```bash
ros2 pkg prefix vr_rm75_teleop
```

确认运行的是当前 workspace。

---

# 三十八、测试要求

每完成一个 Task：

立即跑相关测试。

最后统一跑：

```text
pytest / colcon test
```

以及：

```bash
colcon build --symlink-install
```

根据当前项目实际方式调整。

需要报告：

```text
passed
failed
skipped
```

---

# 三十九、必须专门增加的测试

至少覆盖：

### RM75 state

```text
TCP valid
UDP valid
stale
wrong source
NaN
malformed
wrong joint count
reconnect
```

### Stop policy

```text
controlled stop
safety stop
dual-arm stop
stop timeout
stop ACK
repeated HOLD
```

### Following error

```text
normal
warning
stop
hysteresis
persistence
stale measurement
```

### Collision

```text
self
inter-arm
environment
body
stale
missing geometry
```

### Timing

```text
deadline tracking
measurement age
RTT metrics
```

---

# 四十、非常重要：软件完成 ≠ 真机验证完成

最终报告禁止写：

```text
“真实机械臂安全验证完成”
```

如果你没有真实运行。

状态必须明确区分：

```text
SOFTWARE_COMPLETE
SIMULATED_TEST_PASS
DRY_RUN_READY
HARDWARE_PENDING
HARDWARE_VERIFIED
```

本轮你能够达到的目标最多应该是：

> SOFTWARE_COMPLETE / DRY_RUN_READY

真实：

```text
joint order
真实 controller response
actual RTT
stop distance
following error
UDP frequency
collision geometry accuracy
```

必须保持：

```text
HARDWARE_PENDING
```

直到操作者现场验证。

---

# 四十一、最终必须输出的报告

完成后给我一个结构化报告。

---

## 1. 最终系统结构

包括：

```text
VR
IK
Safety
Collision
State
Command
Stop
Robot
```

完整数据流。

---

## 2. 修改文件

逐文件说明：

```text
文件
修改目的
关键逻辑
```

---

## 3. 新增文件

同样说明。

---

## 4. Test Results

准确输出：

```text
pytest:
xxx passed
xxx failed

colcon:
...

lint:
...
```

不得虚报。

---

## 5. Safety Invariants

列出最终仍然保证的规则。

例如：

```text
no fresh robot state → no motion
no fresh collision snapshot → no motion
no deadman → no motion
NaN → no motion
collision stop → explicit stop
one-arm command fault → both arms stop
```

以最终真实代码为准。

---

## 6. Hardware Pending

必须单独列：

```text
哪些只有真机才能确认
```

---

## 7. 上机操作顺序

给我最终精简版本：

```text
Gate A
→
Gate B
→
Gate C
→
Gate D
```

附实际命令。

---

## 8. 第一次上机时重点观察的日志

明确告诉操作者观察：

```text
哪些 topic
哪些值
正常范围/正常状态是什么
```

不要虚构还未标定的数值范围。

---

# 四十二、你本轮最重要的判断原则

不要问：

> “还能不能再增加几个安全功能？”

而应该问：

> “现在每一层安全逻辑是否已经能够被真实传感器状态驱动，并且能够真实作用到机械臂停止行为？”

真正需要闭环的是：

```text
真实状态
↓
安全判断
↓
明确 stop
↓
真实机器人
↓
真实状态反馈
```

而不是只形成：

```text
软件状态机
↓
HOLD
```

---

# 四十三、最终完成标准

只有达到下面条件，才能将本轮标记为完成：

```text
[ ] Real-state commissioning support ready

[ ] UDP realtime state integrated or clearly blocked with exact reason

[ ] measured qdot path ready

[ ] explicit stop policy implemented

[ ] HOLD / FAULT stop semantics implemented

[ ] dual-arm stop linking implemented

[ ] collision geometry audited

[ ] five-class collision backend implemented

[ ] collision missing/invalid fail-closed

[ ] following error monitor implemented

[ ] measured qdot/qddot diagnostics implemented

[ ] timing / RTT / jitter diagnostics implemented

[ ] unified dry-run launch implemented

[ ] commissioning recorder ready

[ ] STOP_POLICY documentation ready

[ ] COMMISSIONING_CHECKLIST ready

[ ] commissioning joint-step tool is dry-run by default, if implemented

[ ] enable_robot_motion remains false

[ ] no real robot movement was performed

[ ] relevant automated tests pass

[ ] ROS 2 build passes, or exact external dependency blocker documented

[ ] all remaining HARDWARE_PENDING items explicitly listed
```

---

现在开始。

第一步：

> **只阅读和审计当前最新仓库，先输出你的当前架构理解、缺口判断、拟修改文件和详细实施计划。**

然后按照：

```text
Task A
→ Task B
→ Task C
→ Task D
→ Tests
→ Final commissioning report
```

依次完成。

不要在完成计划后等待我逐项确认。

如果发现与上述描述不一致：

> 以当前仓库真实代码为准，并在实施前明确指出差异。

始终保持：

```yaml
enable_robot_motion: false
```

直到整个任务结束。
