
你现在需要继续开发我的 VR 双臂遥操睿尔曼 RM75 项目。

项目 GitHub：

https://github.com/moyunon/vr_teleop.git

本次任务不是重新设计整个项目，也不是推翻现有 IK，而是在目前已经跑通的：

VR world-space pose
→ VR/机械臂坐标映射
→ 双臂连续 IK
→ 零空间优化
→ Cartesian rate limiter
→ target feasibility
→ singularity projection
→ RViz 双臂实时运行

这一基础上，继续完成“真机上机前安全层”和“真实机械臂状态接入”。

请严格按照下面的工作方式执行。

# 一、首先不要直接修改代码

第一步必须先完整阅读当前仓库，尤其重点阅读：

* `vr_rm75_ws/src/vr_rm75_teleop/vr_rm75_teleop/quest_dual_ik_fusion.py`
* `rm75_ik.py`
* `rm75_model.py`
* `rm75_fk.py`
* `rm75_jacobian.py`
* `target_feasibility.py`
* `joint_safety.py`
* `se3_rate_limiter.py`
* `vr_pose_mapping.py`
* `nullspace.py`
* `check_target_feasibility.py`
* `check_continuous_ik.py`
* `check_vr_pose_mapping.py`
* `check_controller_fk.py`
* 其他与 RM75 控制器通信、ROS2 launch、config、URDF 有关的文件

使用 `rg`、目录树、调用关系搜索等方式确认真实代码结构，不要仅根据文件名或 README 猜测。

尤其要确认：

1. `quest_dual_ik_fusion.py` 当前真实的数据流。
2. `q_safe` / `T_safe` 是在哪里产生和更新的。
3. 当前 `q_start` 是否为硬编码。
4. `joint_safety.py` 是否已经接入在线双臂节点。
5. 当前 sigma singularity protection 的完整调用路径。
6. VR tracking loss / stale / recovery 的处理。
7. 当前节点是否存在任何真实机械臂运动命令。
8. RM75 左右臂安装坐标变换。
9. 当前所有测试分别验证了什么、没有验证什么。

在做任何修改之前，先向我输出一份：

## Current Architecture Audit

至少包括：

* 当前完整数据流
* 当前已有的安全措施
* 当前缺失的安全措施
* IK 稳定性现状
* 奇异点保护现状
* 初始姿态对齐现状
* 真机通信现状
* 你准备修改的文件
* 你准备新增的文件
* 潜在风险

然后制定一个分阶段实施计划。

不要在计划完成前直接开始大规模改代码。

---

# 二、总体原则

本阶段的核心目标是：

> 建立 Real Robot Safety & Actuation Layer，但第一阶段默认禁止真实机械臂运动。

不要重新实现已经正常工作的 IK。

不要为了“重构得更漂亮”而无意义地修改已经验证过的代码。

优先复用目前已经经过测试的模块，例如：

* `joint_safety`
* `target_feasibility`
* `se3_rate_limiter`
* `rm75_model`
* `rm75_fk`
* `rm75_jacobian`

但如果发现现有实现存在数学或安全问题，应明确说明原因后再修改。

不要直接复制以前学生留下的遥操代码。

本项目的原则仍然是：

> 从现有项目自身架构出发独立实现，只参考官方接口、硬件参数和设计思想。

暂时不要为了本阶段任务修改当前：

`quest_dual_ik_fusion`

的 INFO 日志格式和已有调试输出，除非确实影响功能。

---

# 三、任务 1：建立 RM75 只读状态接口

这是第一优先级。

硬件：

左臂：

`192.168.127.18:8080`

右臂：

`192.168.127.19:8080`

型号：

RM75-6FB，7DOF。

首先实现一个独立的 RM75 hardware/state interface。

第一阶段必须是：

**READ ONLY**

严禁发送任何运动指令。

至少读取：

* 当前 7 个关节实际位置 `q_measured`
* 机械臂连接状态
* 关节使能状态
* 控制器/机械臂错误状态
* 如果接口支持，读取实时 UDP 状态
* 时间戳
* 数据是否 stale

建议将硬件通信和 IK 节点解耦。

不要把 socket/API 通信代码直接全部塞进：

`quest_dual_ik_fusion.py`

建议形成类似：

`rm75_hardware_interface.py`

或者更合理的独立 ROS2 node。

如果项目已有合适架构，优先遵循现有风格。

需要同时支持左右两台机械臂。

必须具备：

* timeout
* reconnect/error reporting
* malformed data protection
* NaN/Inf protection
* stale detection

第一阶段完成以后：

只允许打印 / 发布机器人实际状态。

**不要发送任何运动命令。**

---

# 四、任务 2：解决真实机械臂初始姿态对齐

当前在线 IK 节点中存在硬编码 `q_start` 初始化。

这对于 RViz 是可以的，但真机存在首帧跳变风险。

目标是将启动过程改为：

连接真实机器人
→ 获取有效 `q_measured`
→ 检查 q 合法
→ `q_safe = q_measured`
→ `T_safe = FK(q_measured)`
→ 等待有效 VR tracking
→ capture VR anchor
→ 从当前真实机械臂姿态开始做相对遥操

必须保证：

第一次获得 VR pose 时机械臂目标位置不发生跳跃。

tracking 丢失然后恢复后：

重新 capture VR anchor，

但机械臂 anchor 必须基于恢复时的真实/安全状态。

禁止使用旧 VR absolute pose 直接追赶。

如果机器人实际关节状态还没有读取成功：

系统不得进入 ENGAGED 状态。

---

# 五、任务 3：增加明确的 Safety Supervisor

不要让：

`Quest → IK → Robot Driver`

直接相连。

增加独立 safety layer。

建议最终的数据流：

VR Pose
→ vr_pose_mapping
→ Cartesian rate limiter
→ IK / feasibility
→ Safety Supervisor
→ Robot Command Interface

同时：

RM75 actual state
→ Safety Supervisor

Safety Supervisor 至少维护：

* `q_measured`
* `q_candidate`
* `q_command`
* joint limits
* joint velocity
* 后续 joint acceleration
* sigma_min
* VR tracking state
* VR stale state
* robot communication state
* robot fault
* command timeout

建立明确状态机，例如：

`INIT`

`READY`

`ENGAGED`

`HOLD`

`FAULT`

含义建议：

INIT：
等待两台机械臂有效状态。

READY：
机械臂状态正常，但尚未允许遥操。

ENGAGED：
允许 VR 驱动目标。

HOLD：
保持当前安全状态，不接受 VR 位移。

FAULT：
检测到严重故障，禁止继续发送运动目标。

状态转换必须明确写在代码里，不要通过多个布尔变量隐式拼凑。

---

# 六、任务 4：增加 deadman / clutch 机制的架构

当前阶段如果 VR topic 中已经包含手柄按键状态，可以实现。

如果当前仓库还没有可靠按钮 topic：

先把 Safety Supervisor 的接口设计出来，不要凭空伪造 Quest 消息。

核心规则：

只有 deadman 持续有效时：

`READY → ENGAGED`

deadman 松开：

立即：

`ENGAGED → HOLD`

机械臂停止跟随 VR。

再次按下时：

重新 capture：

* 当前 VR pose
* 当前机械臂安全 pose

从新的相对零点继续遥操。

绝对不能在重新按下以后追赶用户松开 deadman 期间 VR 手柄移动的距离。

如果现在无法可靠得到按键消息：

把功能接口和状态机实现好，并明确 TODO，不要用假的按钮数据。

---

# 七、任务 5：把关节速度限制正式接入在线链路

这是当前代码已经存在、但双臂融合节点尚未真正使用的重要安全模块。

仔细参考：

`joint_safety.py`

以及：

`check_target_feasibility.py`

中经过测试的处理方式。

完整链路应类似：

IK / projected target
→ `q_candidate`
→ joint velocity limit
→ joint soft limit
→ 得到 `q_command`
→ FK(`q_command`)
→重新计算真实 command 对应的 sigma
→ 更新最终 `q_safe / T_safe`

注意：

IK 中的：

`max_joint_step`

只是求解器内部稳定措施。

**它不能替代真实控制周期下的 joint velocity limiter。**

首轮真机安全参数必须非常保守。

不要直接使用 RM75 最大关节速度。

建议参数化：

`joint_velocity_scale`

第一次默认：

约 `0.10`

即仅允许模型 `qd_max` 的约 10%。

所有参数必须放在 ROS2 参数/config 中，避免散落 magic number。

---

# 八、任务 6：增加 joint soft limits

不能只依赖机械硬限位。

基于 `rm75_model.py` 中已有 hard joint limits 增加：

`teleop soft limits`

soft limits 应严格位于 hard limits 内部。

特别注意 RM75 7DOF 的肘部奇异问题。

J4 接近 0° 时是重要奇异构型之一。

当前左右臂初始姿态分别处于不同 elbow branch，因此不要简单允许：

`q4`

从负值自由穿越 0 到正值，或者相反。

请研究当前左右臂 ready posture / branch，然后设计：

LEFT：

保持其既有 elbow branch。

RIGHT：

保持其既有 elbow branch。

第一版可以使用安全 margin 参数，例如：

`elbow_singularity_margin_deg`

但不要未经分析就写死最终值。

需要：

* 参数化
* 单元测试
* 明确说明 branch enforcement 的逻辑

最终目标：

正常 VR 遥操不能把 J4 直接穿过完全伸直奇异构型。

---

# 九、任务 7：升级 singularity handling

目前已有：

* DLS
* sigma_min
* sigma_stop
* binary projection
* previous solution seed

这些不要推翻。

现在需要从单一 stop threshold 升级成：

### Safe region

`sigma >= sigma_warn`

正常速度。

### Warning region

`sigma_stop < sigma < sigma_warn`

逐渐降低：

* Cartesian translation rate
* Cartesian rotation rate

或者对最终 command 做等价的平滑降速。

要求连续、无明显速度跳变。

### Stop region

`sigma <= sigma_stop`

禁止继续向奇异方向推进。

使用已有 binary projection / hold。

初始可以继续：

`sigma_warn ≈ 0.020`

`sigma_stop ≈ 0.010`

但必须将其作为参数，而不是认为数字已经完全验证。

另外检查当前：

`rm75_jacobian`

的 Jacobian 单位和 SVD 定义。

注意：

平移 Jacobian 和旋转 Jacobian 直接组合以后，sigma 的数值与不同实现之间未必严格可比。

因此增加一个测试脚本：

扫描 RM75 已知奇异附近姿态。

尤其是：

`q4 → 0`

观察：

`sigma_min`

随 q4 的变化。

输出表格或数据：

q4
sigma_min
IK status

确认当前 0.01 / 0.02 阈值在我们的模型中是否合理。

如果需要调整：

先提供测试证据，再修改默认值。

---

# 十、任务 8：评估 adaptive damping

当前 DLS 如果使用固定 damping：

不要直接随意改掉。

先增加测试：

比较：

fixed damping

和：

adaptive damping

在以下区域的表现：

* 正常姿态
* sigma_warn 附近
* sigma_stop 附近
* q4 接近 0

指标：

* IK success
* iteration count
* joint step
* Cartesian error
* joint command continuity
* solve time

只有证明 adaptive damping 明显改善奇异附近稳定性，并且不会破坏正常区域性能后，再接入主链路。

不要为了理论上“更高级”就直接替换已经工作的 DLS。

---

# 十一、任务 9：补充 VR orientation mapping 自动测试

目前 VR mapping 的平移方向已有测试。

需要新增姿态方向测试。

至少验证：

* +roll
* -roll
* +pitch
* -pitch
* +yaw
* -yaw

左右手分别验证。

确认：

`R_current * R_anchor^T`

以及：

`C * R * C^T`

映射后的旋转方向符合：

Quest world frame
→ RM75 left/right base frame

预期。

不要仅检查 quaternion 数值。

应该检查实际 rotation matrix / rotation vector 的轴和符号。

这些测试必须在真机运动前完成。

---

# 十二、任务 10：碰撞安全架构

先检查仓库目前是否已经有 collision model / URDF collision geometry / MoveIt 配置。

不要默认安装 MoveIt。

第一阶段至少设计：

* 单臂 self collision
* 左右臂互撞
* 机械臂与固定环境
* 机械臂与机器人主体

的安全接口。

如果当前 URDF collision geometry 可用：

优先评估是否可以建立轻量级实时距离检测。

对于双臂遥操，不要求第一版实现复杂全局规划。

更重要的是实现：

`distance > d_warn`

正常。

`d_stop < distance <= d_warn`

减速。

`distance <= d_stop`

HOLD。

如果完整 collision checker 工作量较大：

先建立清晰模块边界和测试接口，再逐步实现。

不要把碰撞算法硬塞进 IK solver。

---

# 十三、任务 11：真实 Robot Command Interface，但默认禁止运动

只有在前面的：

* real state
* initialization
* Safety Supervisor
* velocity limit
* soft limit
* singularity protection
* watchdog

均通过测试之后，才能实现 actuator interface。

即使实现以后：

默认参数必须是：

`enable_robot_motion = false`

默认运行项目：

只能：

读取真实机器人状态

* 运行 IK
* 计算 safe command
* 输出 dry-run command

不能真的让机械臂运动。

只有用户显式：

`enable_robot_motion=true`

才允许进入后续硬件测试。

---

# 十四、控制接口选择不要拍脑袋

当前 `quest_dual_ik_fusion` 约 50 Hz：

20 ms/control cycle。

检查 RealMan 官方 API 后再决定：

* `movej_follow`
* `movej_canfd`
* 或其他适合连续遥操的接口

不要因为接口名字看起来像 servo 就直接使用。

特别检查：

* 所需发送频率
* follow 模式要求
* timeout
* blocking/non-blocking
* trajectory planning
* communication failure behavior

如果某接口要求：

≤10 ms

而当前主循环只有 20 ms：

不要直接使用该模式。

请在代码注释和文档中写清楚为什么选择最终接口。

---

# 十五、watchdog 与异常处理

必须实现明确 watchdog。

至少包括：

### VR watchdog

超过设定时间无新 pose：

`ENGAGED → HOLD`

### Robot state watchdog

真实机器人反馈 stale：

进入 FAULT 或 HOLD。

### Command watchdog

如果控制链长时间没有生成新 safe command：

不得继续重复发送旧的运动趋势。

### NaN / Inf

任何：

pose
q
Jacobian
IK result
sigma

出现 NaN/Inf：

立即拒绝 command。

### IK failure

单次失败：

保持上一安全 command。

连续失败达到阈值：

HOLD。

### 双臂协调

如果一侧进入严重 FAULT：

另一侧默认也进入 HOLD/FAULT。

不要出现：

左臂通信断开，而右臂继续跟随 VR 高速运动。

---

# 十六、加速度与 command jump protection

除了 velocity limit，再增加：

joint acceleration / command delta protection。

但注意优先级：

第一版可以先实现：

最大 joint command delta

以及根据控制周期计算 qdot。

随后再实现严格 qddot limiter。

必须防止：

由于 IK branch 变化、数值错误、tracking recovery 等原因，

出现：

`q_command[k] - q_command[k-1]`

异常跳变。

超过阈值时：

reject / HOLD，

不要简单 clip 后继续运动而隐藏异常。

---

# 十七、真机首次运动参数

代码中保留正常参数配置能力，但第一次硬件测试配置必须额外提供：

例如：

`safe_first_motion.yaml`

建议初始：

Cartesian translation：

约 `0.05 m/s`

Cartesian rotation：

约 `0.3 rad/s`

joint velocity：

约 `0.10 * qd_max`

并：

* collision protection ON
* deadman required
* singularity protection ON
* joint soft limits ON
* watchdog ON

不要把这些首轮保守参数写死到算法内部。

---

# 十八、测试要求

每完成一个模块：

立即运行对应测试。

不要等全部改完以后一次性测试。

至少保持现有测试继续 PASS：

* FK
* Jacobian
* IK
* continuous IK
* nullspace
* IK joint centering
* target feasibility
* VR pose mapping

并新增：

### Real state parser/interface tests

模拟：

* 正常数据
* timeout
* malformed packet
* stale
* NaN
* disconnect

### Initialization tests

机器人初始 q 任意改变后：

首次 VR tracking 不得造成 target jump。

### Safety state machine tests

验证：

INIT
→ READY
→ ENGAGED
→ HOLD
→ FAULT

所有合法/非法转换。

### Joint velocity tests

验证：

正常 command

以及：

故意制造过大 q jump。

### Soft limit tests

验证：

所有 7 个 joint。

重点测试：

J4 branch protection。

### Singularity sweep

重点测试：

`q4 → 0`

### VR orientation tests

±roll / ±pitch / ±yaw。

### Watchdog tests

VR stale

robot stale

IK repeated failure。

### Dual arm fault test

左臂 fault 时：

右臂必须按设计进入 HOLD/FAULT。

---

# 十九、修改原则

不要一次性重写大量代码。

优先做小模块、明确接口。

每个阶段：

1. 阅读相关代码
2. 写修改方案
3. 修改
4. 运行测试
5. 修复测试
6. 汇报结果
7. 再进入下一阶段

如果某个现有模块已经正确：

不要为了统一代码风格随意重构。

避免：

* 大量 rename
* 无意义目录调整
* 全项目 formatter 导致巨大 diff
* 同时修改不相关功能

保持 Git diff 清晰。

---

# 二十、每个阶段完成后的汇报格式

每完成一个任务，向我输出：

## Task N Result

### What changed

修改了什么。

### Files changed

列出具体文件。

### Safety rationale

为什么这么设计。

### Tests

执行了哪些命令。

### Test result

PASS / FAIL。

### Remaining risks

仍有什么未解决。

### Next task

下一步准备干什么。

如果测试 FAIL：

先解决或者明确解释。

不要默默跳过失败继续做后面的功能。

---

# 二十一、最终验收目标

这一阶段完成以后，我希望系统至少形成：

Quest
→ world-space pose
→ VR mapping
→ Cartesian rate limiter
→ IK
→ feasibility / singularity protection
→ joint soft limit
→ joint velocity/acceleration safety
→ collision safety interface
→ Safety Supervisor
→ safe joint command
→ RM75 command interface

同时：

RM75 actual state
→ state interface
→ Safety Supervisor
→ initialization / feedback / watchdog

最终必须保证：

### 默认启动

`enable_robot_motion=false`

因此：

可以连接两台真实 RM75，

可以读取真实状态，

可以运行整个：

VR → IK → safety

链路，

但不会发送真实运动指令。

这就是本阶段最重要的验收条件。

---

# 二十二、特别强调：不要提前让机械臂运动

在没有确认以下项目全部正常之前：

* 左右机器人真实 q 读取
* 初始姿态同步
* VR anchor
* joint velocity limiter
* soft joint limits
* singularity warning/stop
* stale watchdog
* Safety Supervisor
* command jump protection
* enable_robot_motion=false 默认保护

不要实际发送任何连续运动 command。

即使已经完成 RM75 actuator API：

也只运行 dry-run。

不要替我擅自执行真实机械臂动作。

---

现在请开始。

第一步：

**完整读取仓库，不修改代码。**

然后先输出：

1. Current Architecture Audit
2. 你发现的现状与我上面描述是否一致
3. 哪些模块其实已经存在、不需要重复开发
4. 哪些地方存在隐藏风险
5. 分阶段实施计划
6. 每个阶段预计修改哪些文件
7. 每个阶段的验收条件

等计划清晰以后，再按照该计划依次实施代码修改和测试。
